# GameNest Shop

Upload `index.html` and `gamenest-logo.png` to the root of your GitHub Pages repository.

The logo is used:
- inside the website
- as the browser tab favicon
- as the mobile home-screen icon where supported

The site is responsive and uses a black / white / gray palette only.
